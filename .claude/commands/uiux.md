---
argument-hint: Component name, page, or file path
---

UI/UX audit on $ARGUMENTS. Check:

1. **Accessibility** — contrast ratios, alt text, keyboard nav, ARIA labels, focus indicators
2. **Responsive** — mobile (<768px), tablet (768-1024px), no horizontal scroll, touch targets 44x44px
3. **States** — loading, empty, error, success, disabled states all handled
4. **Visual consistency** — spacing scale, typography hierarchy, colors match theme system
5. **UX patterns** — form validation feedback, destructive action confirmations, clear navigation

For each issue:
- Severity: Broken | Poor UX | Could improve
- Description of the issue
- Fix recommendation
